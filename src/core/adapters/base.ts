/**
 * Re-export the AbstractAdapter from types for convenience.
 * Adapters import from here.
 */
export { AbstractAdapter } from '../types/adapter';
export type { Token, AnnotatedText, MatchResult, ConversionTarget } from '../types/adapter';
